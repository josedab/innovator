/** Slack adapter — wraps `@slack/bolt` to handle `/innovate` commands in Slack workspaces. */
export { SlackAdapter } from "./slack.js";
/** Discord adapter — wraps `discord.js` to handle `/innovate` commands in Discord servers. */
export { DiscordAdapter } from "./discord.js";
/** Microsoft Teams adapter — wraps `botbuilder` to handle `/innovate` commands in Teams channels. */
export { TeamsAdapter } from "./teams.js";
