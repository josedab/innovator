{
  "name": "innovator",
  "version": "0.3.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "website"
  ],
  "scripts": {
    "predev": "npm run doctor && npm run build --workspace=packages/core || (echo '❌ Core build failed — fix errors in packages/core/ before starting dev' && exit 1)",
    "dev": "npm run dev --workspace=apps/web",
    "dev:core": "npm run dev --workspace=packages/core",
    "dev:all": "concurrently --kill-others-on-fail -n core,web -c blue,green \"npm run dev:core\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=packages/core && npm run build --workspace=apps/cli && npm run build --workspace=apps/web",
    "build:check": "node -e \"const fs = require('fs'); const files = ['packages/core/dist/index.js', 'packages/core/dist/client.js', 'apps/cli/dist/index.js']; const missing = files.filter(f => !fs.existsSync(f)); if (missing.length) { console.error('Missing build outputs:', missing.join(', ')); process.exit(1); } console.log('All build outputs verified.')\"",
    "dev:cli": "npm run start --workspace=@innovator/cli",
    "dev:docs": "npm run start --workspace=website",
    "docs:api": "npm run docs:api --workspace=packages/core",
    "cli": "npx tsx apps/cli/src/index.ts",
    "test": "vitest run",
    "test:ci": "npm run format:check && npm run lint && npm run typecheck && npm run build && npm run build:check && npm run build --workspace=website && npm run test:coverage",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:changed": "vitest run --changed",
    "validate": "npm run typecheck && npm test",
    "check": "npm run lint && npm run typecheck && npm run format:check && npm test && npm run build --workspace=website",
    "lint": "eslint . && npm run lint --workspace=apps/web",
    "lint:fix": "eslint --fix . && prettier --write .",
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p apps/cli && tsc --noEmit -p apps/web",
    "typecheck:core": "tsc --noEmit -p packages/core",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "rm -rf packages/core/dist apps/web/.next apps/cli/dist coverage && find . -name '*.tsbuildinfo' -not -path './node_modules/*' -delete",
    "clean:all": "npm run clean && rm -rf node_modules packages/core/node_modules apps/web/node_modules apps/cli/node_modules",
    "docs:build": "npm run build --workspace=website",
    "doctor": "node scripts/doctor.mjs",
    "prepare": "husky",
    "start": "npm run start --workspace=apps/web",
    "docker:build": "docker build -t innovator .",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "preinstall": "npx only-allow npm"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "@testing-library/dom": "^10.4.1",
    "@typescript-eslint/eslint-plugin": "^8.60.1",
    "@typescript-eslint/parser": "^8.60.1",
    "@vitest/coverage-v8": "^4.1.8",
    "concurrently": "^10.0.3",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix --cache",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
