# Copilot SDK - uses your GitHub Copilot subscription
# No API keys needed if authenticated via `gh auth login`
# Optional: specify a default model
INNOVATOR_DEFAULT_MODEL=gpt-4.1

# Dev server port (default: 3000)
# PORT=3000
